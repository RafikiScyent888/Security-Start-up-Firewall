# The standing instruction files

These two files live at `/root/.claude/` inside the working container and
apply to **every repo in the Cyber Warrior Program**, not just this one.

They are kept here as well because **the container is temporary.** It was
rebuilt on 19 September 2026 and both files were lost along with an entire
Tier 1 build. Anything that is not in a repo does not survive.

| File here | Goes back to |
|---|---|
| `GLOBAL-CLAUDE.md` | `/root/.claude/CLAUDE.md` |
| `exam-prep-coach.md` | `/root/.claude/exam-prep-coach.md` |

If a session ever starts without them — no mention of the never-push rule, the
AAA floor, the hint ladder, the six-option rule or PIN 3693 — copy them back
to those paths and the session will pick them up.
